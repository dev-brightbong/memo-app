import { defineTokens } from "@chakra-ui/react";

const colors = defineTokens({
  colors: {
    common: {
      black: {
        value: "#000000",
      },
      white: {
        value: "#FFFFFF",
      },
    },
  },
});

export default colors;